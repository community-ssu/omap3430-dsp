#!/bin/sh
set -e
# Automatically added by dh_installupstart
# update event file

if [ -e /etc/event.replace.d/dsp ]; then
	[ -d /etc/event.d/ ] || mkdir -p /etc/event.d/
	cp -f /etc/event.replace.d/dsp /etc/event.d/dsp
fi

# initctl stop dsp --no-wait
# initctl start dsp --no-wait

# End automatically added section
